import { useEffect, useMemo, useState } from "react";
import { motion } from "framer-motion";
import { Bus, CalendarDays, Clock, MapPin, Moon, Plus, Search, Sun, Sunset, Users, Wrench } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { useAuth } from "@/contexts/AuthContext";
import { api } from "@/lib/api";
import { cn } from "@/lib/utils";
import { useToast } from "@/hooks/use-toast";
import { socket } from "@/services/socket";

type TicketSeverity = "low" | "medium" | "high";
type Shift = { key: "morning" | "afternoon" | "evening"; label: string; icon: typeof Sun; badgeClass: string; };
interface DriverRoute { id: number; name: string; active: boolean; schedule?: { id: number; time: string; type: "ida" | "volta"; university?: { name: string } | null; }; vehicle?: { id?: number; name?: string | null; model?: string | null; plate: string; capacity: number } | null; city?: { name: string; state: string } | null; points?: { pickupPoint?: { name: string; address?: string | null } }[]; reservations?: unknown[]; }
const SHIFTS: Record<Shift["key"], Shift> = { morning: { key: "morning", label: "Manhã", icon: Sun, badgeClass: "bg-amber-500/15 text-amber-600 dark:text-amber-400 border-amber-500/30" }, afternoon: { key: "afternoon", label: "Tarde", icon: Sunset, badgeClass: "bg-orange-500/15 text-orange-600 dark:text-orange-400 border-orange-500/30" }, evening: { key: "evening", label: "Noite", icon: Moon, badgeClass: "bg-indigo-500/15 text-indigo-600 dark:text-indigo-400 border-indigo-500/30" } };
const SEVERITY_LABEL: Record<TicketSeverity, string> = { low: "Baixa", medium: "Média", high: "Alta" };
function getShift(time: string): Shift { const hour = Number(time?.split(":")[0] ?? "0"); if (hour < 12) return SHIFTS.morning; if (hour < 17) return SHIFTS.afternoon; return SHIFTS.evening; }
function getRouteDay(route: DriverRoute) { return route.schedule?.type === "volta" ? "Volta" : "Ida"; }

export default function DriverSchedule() {
  const { user } = useAuth();
  const { toast } = useToast();
  const driverName = user?.name?.split(" ")[0] ?? "Motorista";
  const city = user?.city?.name ?? "sua cidade";
  const [routes, setRoutes] = useState<DriverRoute[]>([]);
  const [loading, setLoading] = useState(true);
  const [search, setSearch] = useState("");
  const [filterDay, setFilterDay] = useState<string>("Todos");
  const [open, setOpen] = useState(false);
  const [submitting, setSubmitting] = useState(false);
  const [form, setForm] = useState({ vehicleId: "", title: "", description: "", severity: "medium" as TicketSeverity });

  async function loadRoutes(showLoader = true) { try { if (showLoader) setLoading(true); const { data } = await api.get("/driver/routes"); setRoutes(data ?? []); } catch { toast({ title: "Erro ao carregar escala", description: "Não foi possível buscar suas rotas agora.", variant: "destructive" }); } finally { setLoading(false); } }
  useEffect(() => { loadRoutes(); }, []);
  useEffect(() => { const refresh = () => loadRoutes(false); socket.on("route:occupancy-updated", refresh); socket.on("route:capacity-alert", refresh); socket.on("reservation:created", refresh); socket.on("reservation:canceled", refresh); return () => { socket.off("route:occupancy-updated", refresh); socket.off("route:capacity-alert", refresh); socket.off("reservation:created", refresh); socket.off("reservation:canceled", refresh); }; }, []);

  const vehicles = useMemo(() => { const map = new Map<string, { id: string; label: string }>(); routes.forEach((route) => { if (!route.vehicle?.id) return; const id = String(route.vehicle.id); map.set(id, { id, label: `${route.vehicle.plate} • ${route.vehicle.model ?? route.vehicle.name ?? "Veículo"}` }); }); return Array.from(map.values()); }, [routes]);
  const filteredRoutes = useMemo(() => routes.filter((route) => { const universityName = route.schedule?.university?.name ?? route.name; const matchSearch = search.trim() ? universityName.toLowerCase().includes(search.toLowerCase()) || route.name.toLowerCase().includes(search.toLowerCase()) : true; const matchDay = filterDay === "Todos" ? true : getRouteDay(route) === filterDay; return matchSearch && matchDay; }), [routes, search, filterDay]);
  const grouped = ["Ida", "Volta"].map((day) => ({ day, items: filteredRoutes.filter((route) => getRouteDay(route) === day) })).filter((g) => g.items.length > 0);

  function openTicket(vehicleId = "") { setForm({ vehicleId, title: "", description: "", severity: "medium" }); setOpen(true); }
  async function handleSubmit(e: React.FormEvent) { e.preventDefault(); if (!user) return; if (!form.vehicleId) { toast({ title: "Selecione um ônibus", variant: "destructive" }); return; } if (!form.title.trim() || !form.description.trim()) { toast({ title: "Preencha título e descrição", variant: "destructive" }); return; } try { setSubmitting(true); await api.post("/maintenance-tickets", { vehicleId: Number(form.vehicleId), title: form.title.trim(), description: form.description.trim(), severity: form.severity }); toast({ title: "Chamado enviado", description: "A administração foi notificada." }); setOpen(false); setForm({ vehicleId: "", title: "", description: "", severity: "medium" }); } catch (error: any) { toast({ title: "Erro ao enviar chamado", description: error?.response?.data?.error ?? "Tente novamente em instantes.", variant: "destructive" }); } finally { setSubmitting(false); } }

  return <div className="max-w-5xl mx-auto space-y-6"><div className="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-3"><div><h1 className="text-2xl font-heading font-bold text-foreground">Olá, {driverName}! 🚌</h1><p className="text-muted-foreground text-sm">Sua escala definida pela administração de {city}.</p></div><Button onClick={() => openTicket()} className="gap-2 w-full sm:w-auto"><Plus className="w-4 h-4" /> Abrir chamado</Button></div><div className="bg-card border border-border rounded-xl p-3 flex flex-col sm:flex-row gap-3"><div className="relative flex-1"><Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-muted-foreground" /><Input placeholder="Buscar por rota ou universidade..." value={search} onChange={(e) => setSearch(e.target.value)} className="pl-9" /></div><div className="flex flex-wrap gap-1.5">{["Todos", "Ida", "Volta"].map((d) => <button key={d} type="button" onClick={() => setFilterDay(d)} className={cn("px-3 py-1.5 rounded-md text-xs font-medium transition-colors border", filterDay === d ? "bg-primary text-primary-foreground border-primary" : "bg-background text-muted-foreground border-border hover:text-foreground hover:border-primary/40")}>{d}</button>)}</div></div>{loading ? <div className="bg-card border border-border rounded-xl p-10 text-center text-sm text-muted-foreground">Carregando escala...</div> : grouped.length === 0 ? <div className="bg-card border border-dashed border-border rounded-xl p-10 text-center"><CalendarDays className="w-10 h-10 text-muted-foreground mx-auto mb-3" /><p className="font-heading font-semibold text-foreground">{routes.length === 0 ? "Nenhuma escala atribuída" : "Nenhum resultado"}</p><p className="text-sm text-muted-foreground mt-1">{routes.length === 0 ? "Você ainda não foi escalado em nenhuma rota. Aguarde a definição do administrador." : "Tente ajustar a busca ou o filtro."}</p></div> : <div className="grid grid-cols-1 md:grid-cols-2 gap-4">{grouped.map(({ day, items }, dayIdx) => <motion.div key={day} initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: dayIdx * 0.05 }} className="bg-card border border-border rounded-xl p-4 space-y-4"><div className="flex items-center justify-between"><Badge variant="secondary" className="font-heading">{day}</Badge><span className="text-xs text-muted-foreground">{items.length} {items.length === 1 ? "rota" : "rotas"}</span></div>{items.map((route) => { const shift = getShift(route.schedule?.time ?? "00:00"); const ShiftIcon = shift.icon; const point = route.points?.[0]?.pickupPoint; const vehicleId = route.vehicle?.id ? String(route.vehicle.id) : ""; return <div key={route.id} className="space-y-3 pt-3 border-t border-border first:border-t-0 first:pt-0"><div className="flex items-start justify-between gap-2"><p className="font-heading font-semibold text-foreground flex items-center gap-2"><Bus className="w-4 h-4 text-primary" /> {route.schedule?.university?.name ?? route.name}</p><Badge variant="outline" className={`${shift.badgeClass} gap-1`}><ShiftIcon className="w-3 h-3" />{shift.label}</Badge></div><div className="grid grid-cols-1 sm:grid-cols-2 gap-2 text-sm"><div className="rounded-lg bg-muted/40 p-2.5"><div className="flex items-center gap-1.5 text-muted-foreground text-xs mb-1"><Clock className="w-3.5 h-3.5 text-primary" /> Horário</div><p className="text-foreground font-medium">{route.schedule?.time ?? "--:--"}</p><p className="text-xs text-muted-foreground flex items-center gap-1 mt-0.5"><MapPin className="w-3 h-3" /> {point?.name ?? route.name}</p></div><div className="rounded-lg bg-muted/40 p-2.5"><div className="flex items-center gap-1.5 text-muted-foreground text-xs mb-1"><Users className="w-3.5 h-3.5 text-accent" /> Passageiros</div><p className="text-foreground font-medium">{route.reservations?.length ?? 0} confirmados</p><p className="text-xs text-muted-foreground mt-0.5">{route.vehicle ? `${route.vehicle.name ?? "Veículo"} - ${route.vehicle.plate}` : "Veículo não informado"}</p></div></div>{route.vehicle && <Button variant="outline" size="sm" className="w-full gap-2" onClick={() => openTicket(vehicleId)}><Wrench className="w-4 h-4" /> Abrir chamado desta rota</Button>}</div>; })}</motion.div>)}</div>}<Dialog open={open} onOpenChange={setOpen}><DialogContent className="max-w-lg max-h-[90vh] overflow-y-auto"><DialogHeader><DialogTitle className="flex items-center gap-2"><Wrench className="w-5 h-5 text-primary" /> Novo chamado de manutenção</DialogTitle><DialogDescription>Descreva o problema com o máximo de detalhes para acelerar o atendimento.</DialogDescription></DialogHeader><form onSubmit={handleSubmit} className="space-y-4"><div className="space-y-2"><Label>Ônibus</Label><select className="flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm" value={form.vehicleId} onChange={(e) => setForm((s) => ({ ...s, vehicleId: e.target.value }))} required><option value="">Selecione um ônibus...</option>{vehicles.map((vehicle) => <option key={vehicle.id} value={vehicle.id}>{vehicle.label}</option>)}</select></div><div className="space-y-2"><Label>Título</Label><Input placeholder="Ex.: Barulho no freio" value={form.title} onChange={(e) => setForm((s) => ({ ...s, title: e.target.value }))} maxLength={120} required /></div><div className="space-y-2"><Label>Descrição do problema</Label><Textarea placeholder="Descreva o que está acontecendo..." value={form.description} onChange={(e) => setForm((s) => ({ ...s, description: e.target.value }))} rows={5} maxLength={500} required /><p className="text-[11px] text-muted-foreground text-right">{form.description.length}/500</p></div><div className="space-y-2"><Label>Gravidade</Label><div className="grid grid-cols-3 gap-2">{(["low", "medium", "high"] as TicketSeverity[]).map((s) => <button type="button" key={s} onClick={() => setForm((f) => ({ ...f, severity: s }))} className={cn("px-3 py-2 rounded-md text-xs font-medium border transition-colors", form.severity === s ? "bg-primary text-primary-foreground border-primary" : "bg-background text-muted-foreground border-border hover:text-foreground")}>{SEVERITY_LABEL[s]}</button>)}</div></div><DialogFooter><Button type="button" variant="outline" onClick={() => setOpen(false)} disabled={submitting}>Cancelar</Button><Button type="submit" className="gap-2" disabled={submitting}><Plus className="w-4 h-4" /> {submitting ? "Enviando..." : "Enviar chamado"}</Button></DialogFooter></form></DialogContent></Dialog></div>;
}
